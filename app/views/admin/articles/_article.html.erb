     <tr class = "<%= cycle('even', 'odd', :name => 'class') -%>" >
        <td><%= link_to article.title, admin_article_path(article) %></td>
        <td><%= article.category %></td>
        <td><%= nice_date_short article.created_at %></td>
        <td width="200px">
          <%= link_to 'Edit', edit_admin_article_path(article) %>
          <%= link_to image_tag("delete.png"), admin_article_path(article),  :confirm => 'Are you sure?', :method => :delete %>
        </td>
      </tr>

